public enum eGameMode
{
    Running,
    MatchOver,
    Cutscene,
    Pause,
    GameOver
};

public enum ePlayerState
{
    Ready,
    Attacking,
    InAir,
    Blocking,
    Knockdown,
    Dead
};

public enum eCharacter
{
    Claudia,
    Bruno,
};

public enum eAttacks
{
    Punch,
    Bite,
    Dash,
    CCoop,
    BCoop
}

public enum eStage
{
    Stage1,
    Stage2,    
}
